node_modules/
dist/
coverage/
.DS_Store

# Local configuration and credentials
.env
.env.*
!.env.example
*.pem
*.key

# Relay data when RELAY_DATA_DIR points into the repository
.relay/

# Editor and temporary files
.idea/
.vscode/
*.log
*.tmp

# Terminal Control timelines may contain prompts, output, paths, or secrets
*.termctrl
.termctrl/
.termctrl-artifacts/

# Product video sources, captures, review frames, and renders stay local-only
marketing/launch-video/
